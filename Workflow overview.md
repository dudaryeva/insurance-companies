1. data transform
   - remove columns and rows
   - change data types
2. Create measures:

```DAX
Combined Ratio = DIVIDE([Total Claims] + SUM('СК 01 04'[Технічні резерви за класами страхування життя]) + SUM('СК 01 04'[Технічні резерви за класами страхування іншого, ніж страхування життя]), [Total Premiums], 0)
```
```DAX
Equity = SUM('СК 01 04'[Зареєстрований статутний капітал]) + SUM('СК 01 04'[Сплачений статутний капітал])
```
```DAX
Loss Ratio = DIVIDE([Total Claims], [Total Premiums], 0)
```
```DAX
Net Profit = [Total Premiums] - [Total Claims] - SUM('СК 01 04'[Технічні резерви за класами страхування життя]) - SUM('СК 01 04'[Технічні резерви за класами страхування іншого, ніж страхування життя])
```
```DAX
Reinsurance Ratio = DIVIDE(SUM('СК 01 04'[Перестрахові премії за класами страхування життя]) + SUM('СК 01 04'[Перестрахові премії за класами страхування іншого, ніж страхування життя]), [Total Premiums], 0)
```
```DAX
Solvency Ratio = DIVIDE(SUM('СК 01 04'[Прийнятні активи для розрахунку регулятивного капіталу]), SUM('СК 01 04'[Технічні резерви за класами страхування життя]) + SUM('СК 01 04'[Технічні резерви за класами страхування іншого, ніж страхування життя]), 0)
```
```DAX
Total Claims = SUM('СК 01 04'[Страхові виплати за класами страхування життя, з них:]) + SUM('СК 01 04'[Страхові виплати за класами страхування іншого, ніж страхування життя, з них:])
```
```DAX
Total Premiums = SUM('СК 01 04'[Страхові премії за класами страхування життя, з них:]) + SUM('СК 01 04'[Страхові премії за класами страхування іншого, ніж страхування життя, з них:])
```


3. Add visuals and filters
4. Formating
